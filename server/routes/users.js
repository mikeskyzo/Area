var express = require('express');
var router = express.Router();

/* GET users listing. */
router.get('/user/connect', function(req, res, next) {
  res.json({sex : caca});
});

module.exports = router;
